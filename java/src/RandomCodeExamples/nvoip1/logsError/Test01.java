package RandomCodeExamples.nvoip1.logsError;

public class Test01 {
     /*
         @GetMapping(value = "/address")
    public ResponseEntity<AdressDTO> findProfileData(
        @RequestParam(value = "napikey", required = false) String apiKey) {

    try {
        User user = this.service.getAuthenticatedUser(apiKey);

        AdressDTO profileData = this.service.getAdress(user);

        // Registra um log de sucesso
        this.service.createLog("Usuário: " + user.getId() + " da conta " + user.getAccount().getId()
                + " fez a ação: findProfileData", user.getId(), user.getId(), getClientIpAddress(), "findProfileData");

        return ResponseEntity.ok(profileData);
    } catch (TokenNotFoundException e) {
        e.printStackTrace();
        // Registra um log de erro
        this.service.createLog("Erro ao buscar dados de perfil", 0, 0, getClientIpAddress(), "findProfileDataError");
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).build();
    } catch (UnauthorizedProfileException e) {
        e.printStackTrace();
        // Registra um log de erro
        this.service.createLog("Erro ao buscar dados de perfil não autorizado", 0, 0, getClientIpAddress(), "findProfileDataUnauthorizedError");
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED).build();
    }
}



    //TODO exceptions















         */
}
